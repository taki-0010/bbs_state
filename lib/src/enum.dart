enum LaunchStatus {
  loading,
  close,
  start;
}

enum ProcessResults {
  success,
  failed;
}

enum BottomMenu {
  forums,
  history,
  search;
}

enum PrimaryViewState {
  boards,
  threads,
  content;
}

enum ScreenList {
  forums,
  history,
  favorites,
  search;
}

// enum MuteType {
//   userId,
//   postId;
// }

enum OpenLink {
  dialog,
  browser;
}

enum ConnectTo {
  server,
  local;
}
